# Real-model runs only (operator-gated; tests and the mock model need no env).
# These variables configure the OpenAI-compatible endpoint (see PLAN.md §0). Never commit real keys.
SKILL_LAB_BASE_URL=
SKILL_LAB_API_KEY=
SKILL_LAB_MODEL=
