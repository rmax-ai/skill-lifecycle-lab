# Real-model runs only (operator-gated; tests and the mock model need no env).
# These variables override the corresponding `model` keys in configs/default.json
# (see PLAN.md §0). Never commit real keys.
SKILL_LAB_BASE_URL=http://localhost:8000/v1
SKILL_LAB_API_KEY=
SKILL_LAB_MODEL=
SKILL_LAB_TEMPERATURE=
SKILL_LAB_MAX_TOKENS=
SKILL_LAB_TIMEOUT_S=
